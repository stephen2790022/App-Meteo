import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#000", // Black background for digital clock effect
  },
  timeText: {
    fontSize: 80, // Large font for digital clock display
    color: "#0ff", // Cyan for a digital clock feel
    fontWeight: "bold",
  },
});
